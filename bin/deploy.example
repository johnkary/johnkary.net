#!/usr/bin/env bash
#
# Generates static site then deploys to a remote host on Amazon S3

printf "\nDeploy script not yet updated to Amazon S3\n"
#jekyll build && rsync -avz --delete _build/ johnkarydotnetdeploy:/path/to/public/dir
